kind = "web"
previewPath = "/"
title = "NexusDesk — NITK"
version = "1.0.0"
id = "artifacts/nexusdesk"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19211

[services.development]
run = "pnpm --filter @workspace/nexusdesk run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nexusdesk", "run", "build" ]
publicDir = "artifacts/nexusdesk/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19211"
BASE_PATH = "/"
